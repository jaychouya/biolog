# V2EX 发布模板

## 标题

```
开源了一个多模态推理可视化 Demo：同一段症状，加一张照片置信度从 63% 飙到 84%
```

## 正文

```
做了个 side project：Biolog — 多模态 AI 推理链可视化

核心功能：
- 对比实验：纯文本 vs 文本+餐食照片（默认展示 GPT-4o 实录样本）
- 流式推理步骤 + 反事实置信度跳变
- 照片区域标注 + react-flow 推理路径
- 支持 OpenAI / Kimi / 硅基流动等，用户自备 Key

技术栈：React + Vite + Tailwind + FastAPI
演示模式零门槛 · AI 模式用户自备 Key

在线 Demo：[链接]
GitHub：[链接]

重点说明：生活方式关联探索，非医疗建议。Key 经后端转发，不存储。
欢迎 Star 🌟
```

## 节点

- `github` `开源` `ai` `多模态` `react` `fastapi`

## 首条评论

```
补充：
- 首页「演示体验」无需 Key，点开即玩
- 「技术对比」默认是实录样本，有 Key 可一键重跑
- AI 模式可选预设场景（头疼+麻辣烫等），走真实 API
- 深链直达对比：`https://你的域名/?view=compare`
```

## 配图

1. 对比实验页面截图（左右分屏 + 照片标注）
2. 15 秒 GIF：置信度条跳变 + 证据链展开
3. README 中的三张截图
